# Results

This folder contains small summary files for manuscript reporting.

Large generated files should not be committed to GitHub, including:

- `.npz` feature matrices
- `.npy` similarity matrices
- full ROC plot folders
- raw PDB/SDF structures

The summary CSV files contain the final AUC values used in the IEIM analysis.
